#pragma once

enum class SOpcode
{
	none,
	add,
	sub,
	div,
	mul,
	eq,
	lt,
	le,
	ge,
	gt,
	ne,
	not,
	neg,
	and,
	or,
	bz,
	jmp,
	load_class,
	load_const_i,
	load_const_f,
	load_const_str,
	load_idx,
	load_attr,
	load_local,
	load_function,
	load_this,
	set_class,	
	set_attr,	
	set_local,
	set_idx,
	set_this,
	call,
	call_native,
	ret,
	new_table,	
	pop_top,
	push,
	push_this,	
	dup,
	jmp_to_entry,
	inc_bp,	
	new_object,	
	deep_copy,
	push_nil,
	halt,
	break_holder,
	continue_holder
};